include <stdio.h>
#include <stdlib.h>
#define MAX_SIZE 100

int Calc(char exp[i], char op1, char op2){
    int a = op1;
    int b = op2;
    if (exp[i] = '+'){
        return a + b;
    }
    else if (exp[i] = '-'){
        return a - b;
    }
    else if (exp[i] = '*'){
        return a * b;
    }
    else if (exp[i] = '/'){
        return a / b;
    }
}

void Push(char A[], char S[]){
    
}

int main(void){

}